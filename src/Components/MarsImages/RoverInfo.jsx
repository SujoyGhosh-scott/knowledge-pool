import React from "react";

import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({}));

const RoverInfo = ({ data }) => {
  const classes = useStyles();

  return <h1>RoverInfo</h1>;
};

export default RoverInfo;
